{
  "name": "qq-pet",
  "version": "0.1.0",
  "description": "QQ宠物单机复刻(个人学习怀旧用途)",
  "main": "dist/main.js",
  "private": true,
  "engines": {
    "node": ">=18",
    "npm": ">=9"
  },
  "scripts": {
    "build": "node build.mjs",
    "start": "npm run build && electron .",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "hittest": "npm run build && node tools/run-hittest.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "electron": "^31.7.7",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
